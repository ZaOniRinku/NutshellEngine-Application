// Auto-generated by generate_scriptable_factory.py from CMake

#pragma once
#include "../Core/Common/ecs/components/ntshengn_ecs_scriptable.h"
#include <string>

#include "billboardscript.h"
#include "camerascript.h"
#include "footballscript.h"
#include "gamecontrollerscript.h"

namespace NtshEngn {

	namespace ScriptableFactory {

		Scriptable createScriptable(const std::string& scriptName) {
			Scriptable scriptable;

			if (scriptName == "BillboardScript") { scriptable.script = std::make_unique<BillboardScript>(); }
			else if (scriptName == "CameraScript") { scriptable.script = std::make_unique<CameraScript>(); }
			else if (scriptName == "FootballScript") { scriptable.script = std::make_unique<FootballScript>(); }
			else if (scriptName == "GameControllerScript") { scriptable.script = std::make_unique<GameControllerScript>(); }

			return scriptable;
		}

	}

}